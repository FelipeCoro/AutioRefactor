<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        tools:src="@drawable/photo_slider1"
        android:id="@+id/myimage"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:contentDescription="@string/app_name" />

    <TextView
        tools:text="Thousands of stories to make any trip even more interesting"
        style="@style/TextViewLoraBoldTitle"
        android:id="@+id/tvTitle"
        android:layout_marginTop="26dp"
        android:layout_marginHorizontal="24dp"
        android:textSize="18sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:textSize="14sp"
        android:layout_marginTop="8dp"
        tools:text="Explore our collection of 9000+ stories exclusive to HearHere."
        android:textColor="@color/gray_letter"
        android:gravity="center"
        android:layout_marginHorizontal="24dp"
        android:id="@+id/tvSubText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>